pipeline {
    agent any
    environment {
        name="mina"
        country="egypt"
    }


    stages {
        stage("test jenkins") {
        steps {
            echo "hello ${name} from ${country}"
        }
        }
    }
}